import * as express from "express";
import { Router } from "express";

export default function createApiRouter(options: any): Router {
  const router: Router = express.Router();
  // --- TBA: oauth2 endpoints
  return router;
}
